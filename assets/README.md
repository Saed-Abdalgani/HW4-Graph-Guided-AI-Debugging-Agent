# Assets (diagrams & screenshots)

## Obsidian graph view (T3.7)

Add a **screenshot of your Obsidian graph view** (local graph or global graph) for the root `README.md` as:

`assets/obsidian-graph-view.png`

Suggested capture:

1. Open this folder (`obsidian/`) as an Obsidian vault.
2. Open the graph view and frame the bug-1 focus cluster (`test_chinese`, `FileWriter`, `.write()`).
3. Export / screenshot to `assets/obsidian-graph-view.png` (replace any placeholder).

The repository ships without a real UI screenshot so CI stays deterministic and lightweight.
