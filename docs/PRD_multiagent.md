# PRD — Multi-Agent Workflow (`graphdebug`)

> **Status:** stub (Phase 0). Full specification lands in Phase 5 per `todo.md` T5.x / `plan.md` §5.4.

## Scope (to be written)

- `AgentState` lifecycle and supervisor routing (`Command(goto=...)`).
- Worker responsibilities: Navigator, Investigator, Retriever, Fixer, Verifier, Scribe.
- Budget caps, HITL gates, and failure handling.
- Naive vs graph mode differences (fair baseline controls).

## References

- `prd.md` functional requirements **FR-A\***
- `plan.md` §2, §5.4, §6.1
