# Copy to `.env` and fill in values. Never commit real secrets.

# Required for LangChain OpenAI (ADR: single provider SDK)
OPENAI_API_KEY=

# Optional alias if you prefer a project-scoped name
GRAPHDEBUG_OPENAI_API_KEY=
